# `/find` 托管 Everything 设计与实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 UiPilot 安装并管理的 Everything 1.4 Stable Service、隐藏客户端和 Query2 IPC，替换未完成的 Rust 自研文件索引，同时保持现有 `/find` 交互、响应协议和文件执行安全边界。

**Architecture:** 安装器负责一次 UAC、受管二进制和 Windows Service；UiPilot 用户态进程负责启动专用 Everything 客户端、维护 IPC 消息线程并把查询结果适配为现有 `FileSearchResponse`。迁移期通过 `FileSearchBackend` 隔离旧 `FileIndex` 和新 Everything backend，生产切换验收后删除旧索引。

**Tech Stack:** Rust 1.96、Tauri 2.11、`windows` 0.61、Win32 `WM_COPYDATA`/Query2 IPC、NSIS per-machine、PowerShell、React 19、Vitest 4。

## Global Constraints

- 目标平台仅 Windows x64。
- 基线候选为 Everything `1.4.1.1032 x64 Stable`；Iteration 0 记录 SHA-256 后冻结。
- 不采用 Everything 1.5 Beta，不自动跟随上游版本。
- 安装器采用 NSIS per-machine，一次 UAC；UAC 被拒绝时整次安装取消。
- 使用 UiPilot 专用 instance、Service、pipe、配置和数据库目录，不操作用户自己的 Everything。
- 首版 Service pipe 只授权首装 `service_owner_sid`；OTS 桌面标准用户和其他本地用户不支持 `/find`。
- `/find` 只搜索名称和元数据，不搜索文件内容，不开放 Everything 高级语法。
- WebView 不能提交路径、Shell 参数、Everything 表达式、instance 或 pipe。
- 搜索结果最多 200 项，`total` 必须与产品语义一致。
- 日志和验证产物不得包含查询词、文件名、完整路径、用户名或原始 IPC buffer。
- 新旧后端只在开发迁移期并存；生产切换后必须删除旧索引代码与 `rusqlite`。

---

## 一、决策摘要

### 1. 可行性

方案可行。Everything 已覆盖自研索引中最昂贵的固定卷枚举、USN 增量更新、数据库持久化、恢复和排序能力。
UiPilot 仍需自研的部分收敛为：安装与升级、进程监督、Query2 IPC、查询语义适配、结果令牌和故障体验。

### 2. 生产方案

采用 Rust 原生 Query2 IPC：

```text
NSIS Installer (elevated)
  ├─ deploy pinned Everything.exe
  ├─ install/start UiPilot-owned Everything Service
  └─ write protected ownership manifest

UiPilot.exe (normal user)
  ├─ EverythingRuntimeSupervisor
  ├─ EverythingIpcWorker
  ├─ EverythingFileSearch
  └─ ResultRegistry

Everything.exe -instance UiPilot (hidden user process)
  └─ private service pipe
       └─ UiPilot-owned Everything Service
```

Everything SDK DLL 只在独立 Spike 中作为结果对照，不进入首版生产包。`ES.exe`/CLI 每次启动子进程的
方案不进入 Spike。

### 3. 保留与删除

保留：

- `src/protocol.ts` 的文件搜索协议。
- `/find` React UI、类别、排序、键盘和 stale-response 行为。
- `search_files` Tauri command 的调用形状。
- `ResultRegistry` 的 query token 和 opaque `resultId`。
- `execute_result` 的 Rust 内部文件动作分支。

删除：

- 固定卷扫描、USN watcher、SQLite 索引和 integrity worker。
- FileIndex 的 runtime epoch、pause/clean-close/recovery 状态机。
- `rusqlite` 和只服务于旧索引的 Windows features。
- 旧索引性能与 I/O 证据脚本中不再适用的扫描门禁。

## 二、运行时设计

### 1. 状态机

```rust
pub(crate) enum FileSearchRuntimeStatus {
    Starting,
    Ready,
    Partial,
    Rebuilding,
    Unavailable(FileSearchUnavailableReason),
}

pub(crate) enum FileSearchUnavailableReason {
    ComponentMissing,
    ManifestInvalid,
    ServiceUnavailable,
    ClientLaunchFailed,
    IpcUnavailable,
    ProtocolMismatch,
    CrashLoop,
}

pub(crate) enum FileSearchError {
    Unavailable(FileSearchUnavailableReason),
    ConcurrentMutation,
    TieGroupTooLarge,
    Deadline,
    StaleRuntime,
    Restarted,
    ManifestMismatch,
    OwnerMismatch,
    ProtocolMismatch,
    RevisionExhausted,
}
```

映射到现有前端：`Starting -> building`、`Ready -> ready`、`Partial -> partial`、
`Rebuilding -> rebuilding`、所有 `Unavailable(_) -> unavailable`。

### 2. 后端接口

```rust
pub(crate) trait FileSearchBackend: Send + Sync {
    fn runtime_epoch(&self) -> u64;

    fn search(
        &self,
        query: QuerySpec,
        expected_runtime_epoch: u64,
    ) -> Result<AuthenticatedFileSearchBatch, FileSearchError>;

    fn execute(&self, action: &FileResultAction)
        -> Result<FileExecutionOutcome, FileExecutionError>;

    fn shutdown(&self);
}

pub(crate) struct AuthenticatedFileSearchBatch {
    pub(crate) runtime_epoch: u64,
    pub(crate) status: FileSearchStatus,
    pub(crate) total: u32,
    pub(crate) items: Vec<AuthenticatedFileSearchItem>,
    pub(crate) fingerprint: ResultFingerprint,
    // No request/result IDs and no revision.
}
```

迁移期的 backend-neutral action：

```rust
pub(crate) enum FileResultAction {
    Indexed(OpenIndexedPath),
    Everything(AuthenticatedPathAction),
}

pub(crate) struct AuthenticatedPathAction {
    pub(crate) runtime_epoch: u64,
    pub(crate) publication_generation: u64,
    pub(crate) canonical_path: std::path::PathBuf,
    pub(crate) kind: FileResultKind,
    pub(crate) components: Vec<PathComponentIdentity>,
}

pub(crate) struct PathComponentIdentity {
    pub(crate) resolved_prefix: std::path::PathBuf,
    pub(crate) volume_serial: u64,
    pub(crate) file_id: [u8; 16],
    pub(crate) attributes: u32,
    pub(crate) reparse_tag: Option<u32>,
}
```

`OpenIndexedPath` 仅由旧 adapter 使用。Task 5 先把 `windows_backend.rs` 中的 `OwnedHandle`、逐组件遍历、
reparse policy、identity 读取和 Shell dispatch 迁入共享 `file_search/windows/path_auth.rs`，旧 Indexed adapter 与
Everything action 共同委托该模块。执行 admission 必须在同一 backend/runtime 门禁下比较 action 捕获的
`runtime_epoch` 与 `publication_generation`；command 不得临时读取当前 epoch 再作为参数传回形成恒真检查。
共享 helper 逐组件打开不共享 `FILE_SHARE_DELETE` 的 handle，验证最终 resolved volume/path，并持有全部 handles
直到 path-based Shell API 返回。Task 8 只删除旧 adapter、`Indexed` variant 和 `OpenIndexedPath`，不得删除共享模块。

`commands::search_files` 继续使用 `spawn_blocking`，但 state 从 `Arc<FileIndex>` 改为
`Arc<dyn FileSearchBackend>` 的封装类型 `FileSearchService`。

### 3. IPC 请求

```rust
pub(crate) struct EverythingQuerySpec {
    pub(crate) search: Vec<u16>,
    pub(crate) offset: u32,
    pub(crate) max_results: u32,
    pub(crate) request_flags: u32,
    pub(crate) sort: EverythingSort,
    pub(crate) deadline: std::time::Instant,
}

pub(crate) struct QueryReplyRoute {
    pub(crate) reply_hwnd: u32,
    pub(crate) reply_copydata_message: u32,
}

pub(crate) struct CopyDataEnvelope<'a> {
    pub(crate) source_hwnd: isize,
    pub(crate) dw_data: usize,
    pub(crate) payload: &'a [u8],
}

pub(crate) enum EverythingSort {
    DateModifiedAscending,
    DateModifiedDescending,
}

pub(crate) struct EverythingQueryResult {
    pub(crate) total: u32,
    pub(crate) request_flags: u32,
    pub(crate) sort_type: u32,
    pub(crate) items: Vec<EverythingResultItem>,
}

pub(crate) struct EverythingResultItem {
    pub(crate) full_path: String,
    pub(crate) file_name: String,
    pub(crate) attributes: u32,
    pub(crate) size_bytes: Option<u64>,
    pub(crate) modified_filetime: Option<u64>,
}
```

window 层调用 `encode_query2(&EverythingQuerySpec, QueryReplyRoute)` 注入 reply HWND/message；收到
`WM_COPYDATA` 后先验证 source HWND 和 `dwData`，再把 payload 交给
`decode_list2_payload(&[u8])`。payload parser 不生成 query id，并对实际 request flags、sort type、总长度、
item count、字段 offset、UTF-16 终止和整数溢出逐项检查。

Query2 offset 页不是快照。分页采用双遍读取：page size 256、overlap 64；每页校验 total/flags/sort，用
volume serial + file id 去重，并验证 overlap sentinel identity/顺序。读取完整 cutoff tie group 后，从 offset 0
执行第二遍并要求稳定 identity 序列完全相同。漂移最多整次重试 2 次；每遍最多 64 页、16,384 项、32 MiB，
总 deadline 3 秒。耗尽返回 `ConcurrentMutation`，超限返回 `TieGroupTooLarge`，均不计入 crash-loop。

### 4. 原子 refresh transaction

```rust
pub(crate) struct FileRefreshContext {
    query_spec: QuerySpec,
    runtime_epoch: u64,
    publication_generation: u64,
    query_key: FileQueryKey,
    invocation_id: String,
    source_query_sequence: u64,
}

#[serde(rename_all = "camelCase", deny_unknown_fields)]
pub(crate) struct RefreshFilesRequest {
    invocation_id: String,
    expected_request_id: String,
}

#[serde(tag = "kind", rename_all = "camelCase")]
pub(crate) enum RefreshFilesResponse {
    Unchanged { request_id: String, index_revision: String },
    Replaced { response: FileSearchResponse },
    BootstrapRequired,
    Stale { reason: RefreshStaleReason },
}

pub(crate) enum RefreshStaleReason {
    InvocationChanged,
    RequestChanged,
    DomainInvalidated,
    Superseded,
}

pub(crate) struct RefreshToken {
    generation: u64,
    domain_epoch: u64,
    request_id: String,
    fingerprint: ResultFingerprint,
    context: FileRefreshContext,
}

pub(crate) enum RefreshCommit<R> {
    Unchanged { request_id: String, revision: u64 },
    Replaced(R),
    Stale,
}

pub(crate) struct ResultFingerprint(Vec<u8>);
```

当前 file ResultSet 必须拥有 server-owned `FileRefreshContext`；WebView 不能在 refresh 请求中提交 query、category
或 sort。`refresh_files` 的首句门禁是 `require_main_window(&window)?`，随后只用 invocation/current request ID 调用
`begin_preserving_refresh`。该方法验证 active invocation、file domain 和 request ID，返回当前集合绑定的 context，
且不修改 `latest_query_sequence`、`current`、requestId 或 resultId。未知字段由 `deny_unknown_fields` 拒绝；无
current 时返回 `BootstrapRequired`，由前端走有界普通 `search_files` bootstrap，而不是伪造 refresh query。

backend 在 registry 锁外按 token 内的 `QuerySpec` 和 runtime admission 完成查询、双遍分页、逐组件认证和
fingerprint 构建。`commit_refresh_if_current` 在一个锁临界区内重新验证 generation、domain epoch、request ID、
query key、runtime epoch 和 publication generation：fingerprint 不变时 IDs/revision/action 完全不变；变化时才
分配新 IDs、由 registry 为 batch 盖上新的 publication generation 并原子替换 ResultSet；action 的 runtime epoch
来自完成该批认证的 backend admission。普通 query、hide/domain invalidation 或另一 refresh 已提交都会返回
`Stale`；两个并发 refresh 最多一个可替换当前集合。旧 ID 在提交点之前持续有效。

错误分两类：`ConcurrentMutation`、`TieGroupTooLarge`、单次 `Deadline` 是 query-level soft error，只丢 token 并
保留 current；`StaleRuntime`、`Restarted`、`Unavailable`、manifest/owner/protocol mismatch 与
`RevisionExhausted` 是 lifecycle/security error，必须调用 file-domain invalidate，在同一原子步骤递增 revision、
清空 current 并禁止旧 action 执行。初次 starting/unavailable、building 首次 ready、退避恢复且无 current 时，
前端执行有界 bootstrap `search_files`；只有存在 current 才调用 `refresh_files`。

fingerprint preimage 使用版本化、长度前缀 little-endian 编码，包含 query/category/sort、status、total、
canonical path、每个组件 identity、leaf kind、size 和 modified FILETIME；排除 query sequence、requestId、
resultId、Query2 reply id 和内存地址。backend 只返回 canonical fingerprint、status 和 authenticated batch，绝不
分配或递增 revision。`ResultRegistry` 的 file-domain `content_revision` high-water 是唯一 owner：初始发布、普通
query/category/sort 发布、changed refresh replace、lifecycle invalidate 各 checked increment 一次；Unchanged
不增。overflow 时原子清空 current、标记 revision exhausted 并永久 fail-closed，不回绕或从 backend 重建计数。

### 5. 查询语义

`QuerySpec` 继续由 Rust 根据前端枚举生成。用户 query 必须经过 literal translator；match path、regex、
match case 和 whole word 固定关闭。类别表达式只能由 Rust 生成：

```rust
pub(crate) fn compile_everything_search(spec: &QuerySpec) -> Result<Vec<u16>, QueryCompileError>;
```

translator 必须对空白、引号、反斜杠、`!|<>()*?:;` 和 Unicode 建立 fixture。Rust 二次
`fold_name(name).contains(folded_query)` 只能作为一致性断言；若发生不一致，整次查询失败闭合，不能用
有限 over-fetch 伪造精确 `total`。

### 6. 安全所有权

安装器写入只读 manifest；除版本、架构和预先审核的 SHA-256 外，还固定 `instanceName`、`serviceName`、
`servicePipeName`、`iniPath`、`databasePath`、`configDirectory` 和 `binaryRelativePath`。普通 fetch 脚本只能
验证 lock，禁止根据刚下载的 artifact 生成或重写 SHA-256。更新 lock 是独立人工评审变更，并同时核对
Authenticode publisher。安装、升级和卸载均先验证 manifest、Service binary path 和受管目录。

首装 `service_owner_sid` 是 elevated NSIS token 的用户 SID，即实际提供 UAC 管理员凭据的账户。标准用户
通过 OTS 输入另一管理员凭据时，owner 是管理员账户，不是桌面标准用户。覆盖升级必须读取并保留 manifest
中的既有 owner SID；受管 INI/DB/config ACL 与 pipe SDDL 都绑定该 owner。首版不支持其他用户共享搜索。
首版也不实现应用内 repair command，故障 UI 只提示重新运行可信发布渠道的已签名安装包。

## 三、目标文件结构

```text
third-party/everything/{LICENSE.txt,everything.lock.json}
scripts/{fetch-everything.ps1,test-everything-package.ps1,test-everything-installer.ps1}
spikes/everything-ipc/{Cargo.toml,src/,tests/}
src-tauri/src/file_search/{mod.rs,types.rs,query.rs,service.rs}
src-tauri/src/file_search/windows/{mod.rs,path_auth.rs}
src-tauri/src/file_search/everything/{mod.rs,protocol.rs,ipc_worker.rs,runtime.rs,backend.rs}
src-tauri/windows/nsis-hooks.nsh
src-tauri/resources/everything/{Everything.exe,LICENSE.txt,everything.lock.json}
src-tauri/resources/everything/Everything-UiPilot.ini
```

职责规则：`protocol.rs` 只处理字节布局；`ipc_worker.rs` 只处理 HWND/message loop；`runtime.rs` 只处理
进程和健康状态；`query.rs` 只处理产品语义翻译；`backend.rs` 组合组件；`service.rs` 提供 Tauri state。

## 四、实施任务

### Task 1：冻结第三方依赖与供应链

**Files:**
- Create: `third-party/everything/LICENSE.txt`
- Create: `third-party/everything/everything.lock.json`
- Create: `scripts/fetch-everything.ps1`
- Create: `scripts/test-everything-package.ps1`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: 人工评审提交的版本、SHA-256 和 Authenticode publisher lock。
- Produces: 只能验证 lock、不能重写 lock 的可重复下载流程和安装器资源目录。

- [ ] **Step 1: 编写失败的包完整性测试**

测试精确字段、版本、架构、HTTPS 来源、64 位小写 SHA-256、License 和资源文件哈希；资源缺失时退出非零。

- [ ] **Step 2: 运行测试确认失败**

Run: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/test-everything-package.ps1`

Expected: FAIL，报告人工审核的 `everything.lock.json` 或 artifact 不存在。

- [ ] **Step 3: 实现固定版本下载脚本**

先由发布负责人通过独立人工评审提交 lock：核对官方 HTTPS artifact、Authenticode publisher 和第二渠道
获得的 SHA-256。`fetch-everything.ps1` 固定下载 `Everything-1.4.1.1032.x64.zip`，只读取 lock，验证 hash
与 publisher 后复制资源；脚本没有创建、更新或格式化 lock 的代码路径。失败时不修改已有资源。

- [ ] **Step 4: 生成并校验 artifact**

Run: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/fetch-everything.ps1`

Expected: 输出 `EVERYTHING_ARTIFACT_VERIFIED`、版本、架构和已冻结 SHA-256；下载物不匹配时失败。

Run: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/test-everything-package.ps1`

Expected: `EVERYTHING_PACKAGE_PASS`。

- [ ] **Step 5: Commit**

```powershell
git add third-party/everything scripts/fetch-everything.ps1 scripts/test-everything-package.ps1 .gitignore
git commit -m "chore: pin managed Everything runtime"
```

### Task 2：实现 Query2 协议纯解析层

**Files:**
- Create: `spikes/everything-ipc/Cargo.toml`
- Create: `spikes/everything-ipc/src/lib.rs`
- Create: `spikes/everything-ipc/src/protocol.rs`
- Create: `spikes/everything-ipc/tests/protocol.rs`

**Interfaces:**
- Produces: `encode_query2(&EverythingQuerySpec, QueryReplyRoute) -> Result<Vec<u8>, ProtocolError>`。
- Produces: `decode_list2_payload(&[u8]) -> Result<EverythingQueryResult, ProtocolError>`。

- [ ] **Step 1: 写失败的协议 fixtures**

编码 fixture 验证 `reply_hwnd` 和 `reply_copydata_message` 的 wire offset。LIST2 fixture 覆盖空结果、单项、
实际 request flags/sort type、奇数字节 UTF-16、越界 offset、超大 item count、整数溢出和缺失终止符。

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test --manifest-path spikes/everything-ipc/Cargo.toml --test protocol`

Expected: FAIL，协议函数未定义。

- [ ] **Step 3: 实现安全 codec**

禁止把不可信 buffer 直接 cast 为 Rust struct；全部读取通过 little-endian helper、`checked_add`、
`checked_mul` 和边界切片完成，UTF-16 使用 `String::from_utf16`。

- [ ] **Step 4: 运行协议测试并提交**

Run: `cargo test --manifest-path spikes/everything-ipc/Cargo.toml --test protocol`

Expected: PASS；payload parser 不产生 query id，malformed fixtures 全部返回确定的 `ProtocolError`。

```powershell
git add spikes/everything-ipc
git commit -m "spike: parse Everything Query2 protocol"
```

### Task 3：验证真实 IPC 与查询语义

**Files:**
- Create: `spikes/everything-ipc/src/window.rs`
- Create: `spikes/everything-ipc/src/client.rs`
- Create: `spikes/everything-ipc/src/main.rs`
- Create: `spikes/everything-ipc/tests/live_query.rs`
- Create: `spikes/everything-ipc/tests/query_semantics.rs`

**Interfaces:**
- Produces: `EverythingClient::connect(instance: &str, timeout: Duration)`。
- Produces: `EverythingClient::query(query: EverythingQuerySpec)`。

- [ ] **Step 1: 写 hidden-window 生命周期测试**

测试 worker thread 创建/销毁窗口、reply HWND 注入、单调 reply message id、source HWND 验证、`dwData`
关联、1 秒 query timeout、迟到 reply 丢弃和 client 退出后的 pending request 失败。

- [ ] **Step 2: 实现专用 message thread**

使用 `RegisterClassW`、隐藏 HWND、`GetMessageW`/`DispatchMessageW` 和 `WM_COPYDATA`。window proc 先验证
source 等于当前 Everything IPC HWND，再用 `dwData` 查 pending map，最后调用 LIST2 payload parser。

- [ ] **Step 3: 写真实文件树语义测试**

受控文件名覆盖中文、组合字符、空格、引号、反斜杠、`!|<>()*?:;`、文件夹和固定扩展名；逐项比较
Everything 返回与 `fold_name(name).contains(query)`。
另创建至少 320 个修改时间相同的匹配项，并扩展到超过 16,384 项的资源上限。分页期间分别在 cutoff 前
插入、删除和重命名，注入重复页、漏页和持续变化；验证 overlap/sentinel 与双遍一致性检测漂移，有界
重试耗尽返回 `ConcurrentMutation`，超限返回 `TieGroupTooLarge`，且不归类为 runtime/protocol crash-loop。

- [ ] **Step 4: 运行两个 live gates**

Run: `cargo test --manifest-path spikes/everything-ipc/Cargo.toml --test live_query -- --ignored --nocapture`

Expected: 连续 1,000 次请求无崩溃、串线或超时泄漏。

Run: `cargo test --manifest-path spikes/everything-ipc/Cargo.toml --test query_semantics -- --ignored --nocapture`

Expected: items 和 total 与产品语义一致，否则 Iteration 1 No-Go。

- [ ] **Step 5: Commit**

```powershell
git add spikes/everything-ipc
git commit -m "spike: validate Everything IPC semantics"
```

### Task 4：实现 NSIS 安装、升级与卸载 Spike

**Files:**
- Create: `src-tauri/windows/nsis-hooks.nsh`
- Create: `src-tauri/resources/everything/Everything.exe`
- Create: `src-tauri/resources/everything/LICENSE.txt`
- Create: `src-tauri/resources/everything/everything.lock.json`
- Create: `src-tauri/resources/everything/Everything-UiPilot.ini`
- Create: `scripts/test-everything-installer.ps1`
- Modify: `src-tauri/tauri.conf.json`

**Interfaces:**
- Consumes: Task 1 的 lock、License 和 `Everything.exe`。
- Produces: per-machine NSIS 包、固定 INI/DB 路径、首装 credential-owner SID、保持 owner 的升级和精确卸载。

- [ ] **Step 1: 写安装器源码与 VM 门禁**

门禁检查 `installMode=perMachine`、只构建 NSIS、hooks 被引用、Service/instance/pipe/INI/DB 路径固定、
更新与 ETP/HTTP/FTP/托盘关闭、date-modified fast sort 开启、卸载前验证 binary path，并拒绝模糊终止。

- [ ] **Step 2: 运行源码门禁确认失败**

Run: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/test-everything-installer.ps1 -Mode Source`

Expected: FAIL，报告 NSIS hooks 缺失。

- [ ] **Step 3: 配置安装器资源与 hooks**

首装将 elevated NSIS token user SID 冻结为 `service_owner_sid`，即实际提供 UAC 管理员凭据的账户。OTS
桌面标准用户不是 owner。使用 `-install-service-security-descriptor` 授权 SYSTEM、Administrators 和 owner，
并把 INI/DB/config ACL 绑定 owner。覆盖升级读取并保留既有 manifest owner；另一管理员运行升级不得重写。
卸载 hook 可由管理员执行，但只在验证 manifest/binary path 后精确删除。应用内不保留 installer helper。

- [ ] **Step 4: 构建并执行 VM 矩阵**

Run: `npm.cmd run tauri build -- --bundles nsis`

Expected: 生成一个 x64 NSIS 安装器。

Run: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/test-everything-installer.ps1 -Mode VmMatrix`

Expected: 干净 VM、已有个人 Everything、标准用户 + 不同管理员 OTS、另一管理员覆盖升级/卸载、UAC 取消
全部通过。OTS owner 为凭据管理员，桌面标准用户 unavailable；升级前后 owner SID 和目录 ACL 完全不变；
未授权账户不能枚举；owner query probe 返回 DB loaded、必需 flags 和实际 date-modified sort。

- [ ] **Step 5: Commit**

```powershell
git add src-tauri/tauri.conf.json src-tauri/windows scripts/test-everything-installer.ps1
git commit -m "spike: manage Everything from NSIS"
```

### Task 5：建立生产 `file_search` 适配层

**Files:**
- Create: `src-tauri/src/file_search/mod.rs`
- Create: `src-tauri/src/file_search/types.rs`
- Create: `src-tauri/src/file_search/service.rs`
- Create: `src-tauri/src/file_search/windows/mod.rs`
- Create: `src-tauri/src/file_search/windows/path_auth.rs`
- Modify: `src-tauri/src/lib.rs`
- Modify: `src-tauri/src/commands.rs`
- Modify: `src-tauri/src/result_registry.rs`
- Modify: `src-tauri/src/file_index/windows_backend.rs`
- Modify: `src-tauri/Cargo.toml`

**Interfaces:**
- Produces: `FileSearchBackend` trait 和 `FileSearchService(Arc<dyn FileSearchBackend>)`。
- Produces: backend-neutral `FileResultAction::{Indexed, Everything}`。
- Produces: server-owned `FileRefreshContext`、`ResultRegistry::{begin_preserving_refresh,
  commit_refresh_if_current, invalidate_file_domain}`、唯一 file-domain revision high-water 和 `RefreshCommit`。
- Consumes: 现有 `QuerySpec` 与 ResultRegistry 发布流程；旧 `FileSearchBatch` 拆为不含 revision/opaque IDs 的
  `AuthenticatedFileSearchBatch`，`OpenIndexedPath` 只留在旧 adapter。

- [ ] **Step 1: 为 fake backend 写失败测试**

测试 `search_files_with` 使用 trait backend、保持 `spawn_blocking`、检查 runtime epoch，并在 stale token 时不
发布；fake Everything action 不构造 SQLite row id 或旧 `VolumeIdentity`。ResultRegistry 测试必须证明：
未变化 refresh in-flight/完成期间旧 requestId/resultId 可执行且 IDs 完全不变；变化 refresh in-flight 期间旧
ID 可执行，原子 commit 后才 stale。正确 expectedRequestId 下篡改 query/category/sort 必须被拒绝或完全无效；
普通 query 与 refresh、hide/domain invalidation、两个并发 refresh 的竞态均验证 stale 门禁。soft error 保留
current；runtime/lifecycle/security error 原子 invalidate。revision 序列覆盖初始发布、普通新查询、unchanged、
changed、restart/unavailable 与 overflow，证明只有 registry 分配且不回绕。

- [ ] **Step 2: 运行 command 定向测试确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml commands::tests::file_search_backend`

Expected: FAIL，`FileSearchService` 未定义。

- [ ] **Step 3: 移动共享类型并实现旧后端 adapter**

先把 `src-tauri/src/file_index/windows_backend.rs` 的 `OwnedHandle`、组件遍历、reparse policy、identity 读取和
path-based Shell dispatch 迁移/泛化到 `file_search/windows/path_auth.rs`，让旧 Indexed adapter 改为委托共享
模块，再让 `FileIndexBackend` 生成 `FileResultAction::Indexed`。共享模块单测/竞态测试覆盖父目录 rename、
junction/reparse substitution、leaf 替换、跨卷和 Shell 调用期间 rename，并保持所需 Windows features：
`Win32_Foundation`、`Win32_Storage_FileSystem`、`Win32_UI_Shell`、`Win32_UI_Shell_Common`。

`commands.rs` 和 `result_registry.rs` 改依赖 backend-neutral action。ResultSet 保存 `FileRefreshContext`；实现
preserve-current token、单一 revision high-water 与错误分类。begin 不清空 current，commit 在单锁临界区比较
fingerprint；不变不分配 ID，变化才原子 swap。`AuthenticatedPathAction` 捕获 runtime epoch、publication
generation 和完整组件链；执行 admission 比较 action 自身身份。

- [ ] **Step 4: 运行 Rust 与前端回归**

Run: `cargo test --manifest-path src-tauri/Cargo.toml file_search`

Run: `cargo test --manifest-path src-tauri/Cargo.toml commands::tests`

Run: `npm.cmd test -- src/launcher.test.tsx`

Expected: 现有 `/find` 协议和交互测试全部通过。

- [ ] **Step 5: Commit**

```powershell
git add src-tauri/src/file_search src-tauri/src/file_index/windows_backend.rs src-tauri/src/lib.rs src-tauri/src/commands.rs src-tauri/src/result_registry.rs src-tauri/Cargo.toml src-tauri/Cargo.lock
git commit -m "refactor: isolate file search backend"
```

### Task 6：实现生产 Everything runtime 与 backend

**Files:**
- Create: `src-tauri/src/file_search/query.rs`
- Create: `src-tauri/src/file_search/everything/mod.rs`
- Create: `src-tauri/src/file_search/everything/protocol.rs`
- Create: `src-tauri/src/file_search/everything/ipc_worker.rs`
- Create: `src-tauri/src/file_search/everything/runtime.rs`
- Create: `src-tauri/src/file_search/everything/backend.rs`
- Modify: `src-tauri/Cargo.toml`

**Interfaces:**
- Produces: `EverythingRuntimeSupervisor::start(ManagedEverythingConfig)`。
- Produces: `EverythingFileSearch::new(supervisor, worker)`，实现 `FileSearchBackend`；ResultRegistry 仍只由 command 层持有。

```rust
pub(crate) struct ManagedEverythingConfig {
    pub(crate) binary_path: std::path::PathBuf,
    pub(crate) manifest_path: std::path::PathBuf,
    pub(crate) ini_path: std::path::PathBuf,
    pub(crate) database_path: std::path::PathBuf,
    pub(crate) config_directory: std::path::PathBuf,
    pub(crate) instance_name: String,
    pub(crate) service_name: String,
    pub(crate) service_pipe_name: String,
    pub(crate) service_owner_sid: String,
}
```

- [ ] **Step 1: 移植并扩展 Spike 单元测试**

协议 envelope/payload fixtures、query translator、实际 flags/sort 协商、DB loaded、fast sort、runtime state、
crash-loop、query timeout、迟到 reply、manifest/owner mismatch、canonical fingerprint、双遍分页、资源上限、
共享逐组件身份认证和 shutdown 顺序先失败。backend 测试必须断言输出不含 revision，且 action 捕获 runtime
epoch/publication generation。

- [ ] **Step 2: 运行定向测试确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml file_search::everything`

Expected: FAIL，新模块尚未实现。

- [ ] **Step 3: 实现 runtime supervisor**

启动时验证 binary、manifest、INI、DB/config 路径和 `service_owner_sid`；使用固定 private instance/pipe/INI/DB
参数后台启动无托盘客户端。进入 Ready 前 probe DB loaded、必需 request flags、实际 sort type、fast sort、
版本和无网络服务；任一不符为 `ProtocolMismatch`。搜索路径绝不触发 UAC 或 Service 安装。

- [ ] **Step 4: 实现 Query2 backend**

串行发送请求，只保留最新未发送查询；解析实际 flags/sort。分页每页 overlap 64，验证 sentinel、total、
flags/sort，用稳定 identity 去重；读取到 tie boundary 后整段重读，漂移有界重试。超限返回
`TieGroupTooLarge`，持续变化返回 `ConcurrentMutation`，不得增加 crash-loop。发布前调用 Task 5 的共享
`file_search/windows/path_auth.rs` 生成 `AuthenticatedPathAction` 和 canonical fingerprint；不得复制
`pin_indexed_path_components`。backend 只返回 authenticated batch/status/fingerprint，不持有 revision high-water。

- [ ] **Step 5: 运行测试与 clippy**

Run: `cargo test --manifest-path src-tauri/Cargo.toml file_search::everything`

Run: `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`

Expected: PASS，无 warning。

- [ ] **Step 6: Commit**

```powershell
git add src-tauri/src/file_search src-tauri/Cargo.toml src-tauri/Cargo.lock
git commit -m "feat: query managed Everything over IPC"
```

### Task 7：切换 command、执行与前端故障体验

**Files:**
- Modify: `src-tauri/src/lib.rs`
- Modify: `src-tauri/src/commands.rs`
- Modify: `src-tauri/src/result_registry.rs`
- Modify: `src-tauri/build.rs`
- Modify: `src-tauri/capabilities/main.json`
- Create: `src-tauri/permissions/autogenerated/refresh_files.toml`
- Modify: `src/protocol.ts`
- Modify: `src/main.ts`
- Modify: `src/launcher-core.ts`
- Modify: `src/launcher-view.tsx`
- Modify: `src/launcher.test.tsx`

**Interfaces:**
- Consumes: Task 6 的 `EverythingFileSearch`。
- Produces: `refresh_files` preserve-current command、安装缺失、`/find` 活跃轮询和重新安装提示。

- [ ] **Step 1: 写端到端状态测试**

覆盖无 current 的 bootstrap、starting 不阻塞输入、building 首次 ready 与退避恢复走有界 `search_files`、存在
current 后 ready 每 1 秒调用 `refresh_files`、离开 `/find` 停止。增加原子凭证测试：未变化轮询 in-flight/完成
期间 Enter 成功且所有 IDs 不变；变化轮询 in-flight 期间旧 ID 成功，commit 后才 stale；soft error 保留旧集合，
restart/unavailable/manifest/owner/protocol mismatch 清空并禁止执行。同路径同 metadata 但 file ID 或父组件
identity 变化必须替换。另测正确 request ID + 篡改 query/category/sort、普通 query/refresh 竞态、hide/domain
invalidation、两个并发 refresh，以及 action 捕获 runtime epoch/generation 的 stale execution。

- [ ] **Step 2: 运行测试确认失败**

Run: `npm.cmd test -- src/launcher.test.tsx`

Expected: 新的 unavailable/active-poll cases FAIL。

- [ ] **Step 3: 接入生产 backend 和状态 copy**

新增 `refresh_files` Tauri command：首句执行 `require_main_window(&window)?`；请求采用 deny-unknown-fields 的
`RefreshFilesRequest { invocationId, expectedRequestId }`，不接受 query/category/sort。随后
`begin_preserving_refresh` 从当前 ResultSet 取得 server-owned `FileRefreshContext`，registry 锁外按其 QuerySpec
查询和认证，再 `commit_refresh_if_current`。响应是 discriminated union：`Unchanged` 只返回原 requestId/revision，
`Replaced` 返回完整新 response，`BootstrapRequired` 指示无 current 时走有界普通 `search_files`，`Stale`
区分 invocation/request/domain/superseded。soft error 不提交；lifecycle/security error 调用
`invalidate_file_domain`。`lib.rs` 注册 command/invoke handler，`build.rs`
生成精确 permission，`capabilities/main.json` 只增加 `allow-refresh-files`，`src/protocol.ts` 与 `src/main.ts` 接线。
`launcher-core.ts` 仅在存在 current 时每秒 refresh 并保持原选择；删除旧 change event 链。首版没有 repair
command 或 installer helper。

- [ ] **Step 4: 运行 Rust、前端和构建验证**

Run: `cargo test --manifest-path src-tauri/Cargo.toml commands::tests`

Run: `cargo test --manifest-path src-tauri/Cargo.toml result_registry::tests`

Run: `cargo test --manifest-path src-tauri/Cargo.toml file_search`

Run: `npm.cmd test -- src/launcher.test.tsx`

Run: `npm.cmd run build`

Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```powershell
git add src-tauri/src/lib.rs src-tauri/src/commands.rs src-tauri/src/result_registry.rs src-tauri/build.rs src-tauri/capabilities/main.json src-tauri/permissions/autogenerated/refresh_files.toml src/protocol.ts src/main.ts src/launcher-core.ts src/launcher-view.tsx src/launcher.test.tsx
git commit -m "feat: switch find to managed Everything"
```

### Task 8：删除自研索引与旧依赖

**Files:**
- Delete: `src-tauri/src/file_index/mod.rs`
- Modify: `src-tauri/src/lib.rs`
- Modify: `src-tauri/src/commands.rs`
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/Cargo.lock`
- Modify: `scripts/test-find-performance.ps1`
- Modify: `scripts/test-find-evidence.ps1`

**Interfaces:**
- Consumes: Task 7 已成为唯一生产 backend。
- Produces: 无可达旧索引路径、无 `rusqlite`、无扫描/USN/SQLite 生命周期代码的发布候选。

- [ ] **Step 1: 写旧引擎缺席门禁**

测试生产源码不含 `mod file_index`、`rusqlite`、旧 DB schema、scanner/watcher 入口和旧 Tauri state；同时确保
ResultRegistry、查询类型和 Everything 文件执行仍存在于 `file_search`；`FileResultAction::Indexed`、
`OpenIndexedPath`、SQLite row id 和旧 `VolumeIdentity` 均无生产命中。

- [ ] **Step 2: 删除旧引擎并迁移仍有价值的测试**

把 query parsing、类别、cutoff tie-group 排序、结果发布和 Everything identity 执行测试迁入
`file_search`/`commands`；删除 `Indexed` variant、`OpenIndexedPath` 及只验证扫描、USN、SQLite、clean-close
和旧 recovery 的测试。

只删除 `file_index` adapter；Task 5 迁入的 `file_search/windows/path_auth.rs`、`OwnedHandle`、逐组件认证与 Shell
dispatch 必须保留。源码门禁还要证明旧 `windows_backend.rs` 已无复制实现，且 Everything execution 只委托共享
path-auth 模块；复核 Cargo 的上述四个 Windows features 未被随旧索引依赖误删。

执行安全测试必须复用泛化后的逐组件 pin/auth helper，覆盖父目录 rename、父目录被 junction 替换、reparse
target substitution、leaf 同路径替换、跨卷替换和 Shell API 调用期间 rename 尝试；所有组件 handles 在
Shell API 返回前保持打开且不共享 `FILE_SHARE_DELETE`。

- [ ] **Step 3: 更新性能与证据脚本**

性能改测 warm query、client cold start、首次 DB ready、进程恢复和组合 working set；证据改测无网络监听、
无敏感日志、用户 Everything 隔离和卸载无残留。

- [ ] **Step 4: 运行全量验证**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`

Run: `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`

Run: `npm.cmd test`

Run: `npm.cmd run build`

Expected: 全部 PASS；`rg -n "file_index|rusqlite" src-tauri/src src-tauri/Cargo.toml` 无生产命中。

- [ ] **Step 5: Commit**

```powershell
git add -A src-tauri scripts/test-find-performance.ps1 scripts/test-find-evidence.ps1
git commit -m "refactor: remove self-managed file index"
```

### Task 9：发布候选与受控上线

**Files:**
- Create: `docs/superpowers/verification/2026-07-23-find-everything-migration.md`

**Interfaces:**
- Produces: 可审计的 Go/No-Go 证据和回滚步骤。

- [ ] **Step 1: 执行安装矩阵**

覆盖 Windows 10/11 x64、干净机器、已有用户 Everything、credential-owner 与未授权第二用户、覆盖升级、安装中断、
UAC 取消、Service 停止、客户端崩溃、sleep/resume、重新运行签名安装包和卸载。

身份矩阵单独覆盖：标准用户从桌面启动安装并用另一管理员完成 OTS；确认 owner 是管理员凭据账户、桌面
标准用户 unavailable；再由不同管理员执行覆盖升级和卸载，确认升级不改变 owner SID/ACL，卸载只删除
既有 ownership manifest 指向的资源。

- [ ] **Step 2: 执行安全与隐私门禁**

验证普通用户不能替换受管 binary/manifest；未授权本地账户不能连接 Service pipe 或枚举文件名；无
ETP/HTTP/FTP 监听；日志和 artifacts 无 query/path；卸载只删除 UiPilot Service/pipe/config；错误 source
HWND、错误 `dwData`、畸形 LIST2、flags/sort 不匹配全部失败闭合。

ResultRegistry/前端门禁验证：未变化 refresh in-flight/完成时当前 Enter 始终成功且 requestId/resultId 字节
完全相同；变化 refresh in-flight 时旧 ID 成功，commit 后才 stale；单次 deadline、`ConcurrentMutation`、
`TieGroupTooLarge` 保留当前 ResultSet，restart/unavailable/manifest/owner/protocol mismatch 则原子清空并禁止
执行。同路径同 metadata 但 file ID/父组件 identity 改变必须替换；revision 单 owner 序列与 overflow fail-closed
均有断言。

- [ ] **Step 3: 执行性能门禁**

分别记录 warm query p50/p95/p99、client cold start、首次 DB ready、recovery 和 UiPilot/client/Service 组合
working set，不用 warm 指标掩盖首次启动成本；另记录活跃 query 轮询间隔、文件变化到 UI 刷新的延迟，
以及超过 200 个同时间戳结果的 tie-group 分页成本。
记录双遍分页重试次数、最大项数/内存/deadline 命中和 `ConcurrentMutation`/`TieGroupTooLarge` 计数；这些
查询级错误不能增加客户端 crash-loop 计数。

- [ ] **Step 4: 写验证报告并作 Go/No-Go**

报告必须列出固定版本和 hash、测试命令、样本数、失败项、已知限制、kill switch 和上一版本回滚步骤。
任何用户 Everything 被影响、未授权账户能枚举文件名、Service 残留、flags/sort 协商不一致、普通文件
变化不刷新、刷新导致 IDs 抖动、fingerprint 漏掉执行身份、分页漂移污染当前集合、逐组件 pinning 退化、
owner SID 被覆盖升级改写、查询语义不一致或持续 crash-loop 都判定 No-Go。

- [ ] **Step 5: Commit**

```powershell
git add docs/superpowers/verification/2026-07-23-find-everything-migration.md
git commit -m "test: verify managed Everything migration"
```

## 五、阶段门禁

| 阶段 | 必须通过 | 失败处理 |
|---|---|---|
| Iteration 0 | License、人工审核 SHA-256/签名、fetch 只验证 lock | 不进入 IPC 集成 |
| Iteration 1 | Query2、双遍分页、漂移/超限错误、1,000 次稳定性、literal/total | No-Go 或重新批准产品语义 |
| Iteration 2 | 固定 INI/DB、credential-owner SID/ACL、OTS 与另一管理员升级矩阵 | 不接入生产 command |
| Iteration 3–4 | preserve-current refresh、canonical identity、逐组件 pinning、现有 `/find` 契约 | 不删除旧引擎 |
| Iteration 5 | 全量测试、旧引擎不可达、无 `rusqlite` | 不生成发布候选 |
| Iteration 6 | 灰度无碰撞、残留、敏感日志或 crash-loop | 启用 kill switch 并回滚 |

## 六、明确的 No-Go 条件

- Everything 主程序再分发许可或第三方声明无法确认。
- 不能创建与用户 Everything 完全隔离的 instance、Service、pipe、config 和 DB。
- 普通文本无法安全映射且 `total` 无法保持精确。
- Query2 无法验证 reply HWND、source HWND、`dwData`、实际 request flags 或 sort type。
- LIST2 parser 无法对畸形数据失败闭合。
- 超过 200 个相同时间戳结果时无法得到全局稳定的前 200 项。
- Query2 offset 分页无法通过 overlap sentinel、稳定身份和双遍重读检测漂移。
- `ConcurrentMutation`、`TieGroupTooLarge` 或 timeout 会污染/替换当前 ResultSet，或被计入 crash-loop。
- 未变化 refresh 会清空/重建 IDs，变化 refresh 无法在单一提交点原子替换。
- fingerprint 未包含逐组件执行身份或 revision overflow 不能失败闭合。
- 无法泛化现有逐组件 pinning 并把 handles 保持到 Shell API 返回。
- 活跃 `/find` 无法在有界时间内发现新增、删除或重命名。
- 无法用 Service security descriptor 阻止未授权本地账户枚举文件名。
- 覆盖升级无法验证并保留首装 credential-owner SID 与 INI/DB/config ACL。
- 安装升级可能留下不可识别 Service，或需按进程名模糊清理。
- `/find` 需要在查询时弹 UAC、安装 Service 或修改系统配置。
- 生产切换后仍需长期保留两套索引作为 fallback。

## 七、执行顺序

严格按 Task 1 → 9 执行。Task 1–4 是独立 Go/No-Go Spike；只有前三个阶段评审通过后才允许 Task 5–7
接入生产。Task 8 删除旧引擎不可提前，Task 9 的验证证据不可由单元测试替代。

设计依据见 `docs/superpowers/specs/2026-07-23-find-everything-managed-migration-design.md`。
